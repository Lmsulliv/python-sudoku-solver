# python-sudoku-solver
simple little python sudoku solver I made using recursion

After running the file, just enter the clues you already have in the (row, column, value) format from 1-9 and it will solve the puzzle.
